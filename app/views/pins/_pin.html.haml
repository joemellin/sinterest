%tr
  %td= image_tag pin.image(:thumb)
  %td= pin.description
  %td= link_to 'Show', pin 
  - if current_user == pin.user
    %td= link_to 'Edit', edit_pin_path(pin)
    %td= link_to 'Delete', pin, :method => :delete, :data => { :confirm => 'Are you sure?' }